package pl.webd.dawid124.ioengine.module.action.model.rest;

import java.io.Serializable;

public enum EActionType implements Serializable {
    CHANGE, UP, DOWN, DIM_UP, DIM_DOWN, ON, OFF, TEMP_ON
}
